# Origin of izichat-backend. Baked into the bundle at build time by Vite, so it
# must be set before `npm run build` — never point a deployed build at localhost.
# Defaults to https://api.izichat.ru when unset (see src/lib/api.js).
VITE_API_BASE_URL=https://api.izichat.ru
